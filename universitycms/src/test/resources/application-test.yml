spring:
  flyway:
    locations: classpath:db/test-migration/postgres
  datasource:
    url: jdbc:tc:postgresql:13:///test
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
    hikari:
      minimum-idle: 1
      maximum-pool-size: 5
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
